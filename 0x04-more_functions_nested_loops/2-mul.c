#include "main.h"
/**
 * mul - multiplies two integers
 * @a: 1st int to multiply
 * @b: 2nd int to multiply
 *
 * Return: the result of the multi
 */
int mul(int a, int b)
{
	a = a * b;

	return (a);
}
